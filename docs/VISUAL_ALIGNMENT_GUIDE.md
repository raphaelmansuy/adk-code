# Visual Alignment Guide: Google ADK Go vs adk-code
**For**: Quick reference, architecture reviews, design decisions  
**Created**: November 15, 2025

---

## Architecture Comparison at a Glance

### Google ADK Go Architecture

```
┌─────────────────────────────────────────┐
│         User Application                │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│    Runner (agent.Run orchestration)     │
│  - Manages session                      │
│  - Yields events in iterator pattern    │
│  - Handles tool invocations             │
└────────────┬────────────────────────────┘
             │
             ▼
    ┌────────┴─────────┐
    │                  │
    ▼                  ▼
┌──────────┐       ┌──────────┐
│ LLM      │       │ Tools    │
│ Models   │       │ System   │
│ (Gemini, │       │(function │
│ OpenAI)  │       │ tool,    │
└──────────┘       │ agent    │
                   │ tool)    │
                   └──────────┘
             │
    ┌────────▼─────────┐
    │                  │
    ▼                  ▼
┌──────────────┐  ┌──────────────┐
│Session       │  │Memory &      │
│- Events      │  │Artifacts     │
│- State       │  │- Search      │
│- History     │  │- Storage     │
└──────────────┘  └──────────────┘
```

### adk-code Architecture

```
┌─────────────────────────────────────────┐
│         User (REPL)                     │
│  /command arguments                     │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│    REPL (interactive loop)              │
│  - Parse commands                       │
│  - Show results                         │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│    Agent Discovery & Orchestration      │
│  - Scan .adk/agents/                    │
│  - Parse YAML + Markdown                │
│  - Validate & Lint                      │
└────────────┬────────────────────────────┘
             │
    ┌────────┴──────────┐
    │                   │
    ▼                   ▼
┌──────────┐       ┌──────────────┐
│Execute   │       │Convert to    │
│Agent     │       │Tools (TBD)   │
│(process) │       │(Incomplete)  │
└──────────┘       └──────────────┘
    │
    ▼
┌──────────────┐  ┌──────────────┐
│LLM Provider  │  │Google ADK    │
│(Gemini,      │  │Framework     │
│ OpenAI,      │  │(Integration) │
│ Ollama)      │  │              │
└──────────────┘  └──────────────┘
             │
    ┌────────▼─────────┐
    │                  │
    ▼                  ▼
┌──────────────┐  ┌──────────────┐
│Session       │  │(Planned)     │
│Manager       │  │Memory &      │
│- Persist     │  │Artifacts     │
│- Events      │  │              │
└──────────────┘  └──────────────┘
```

---

## Feature Alignment Matrix

```
                        Google ADK      adk-code        Alignment
                        ──────────      ────────        ─────────

Agent Model             ✅ Code-based   ✅ File-based   DIFFERENT
Execution               ✅ Event stream ⚠️ Process      MISALIGNED
Tool System             ✅ Complete     ⚠️ Partial      INCOMPLETE
Session Mgmt            ✅ Complete     ⚠️ Partial      INCOMPLETE
Memory/Search           ✅ Yes          ❌ No           MISSING
Artifacts               ✅ Yes          ❌ No           MISSING
Discovery               ❌ No           ✅ Yes          UNIQUE
File Format             ❌ No           ✅ Yes          UNIQUE
Version Mgmt            ❌ No           ✅ Yes          UNIQUE
Linting                 ❌ No           ✅ Yes          UNIQUE
```

---

## Execution Flow Comparison

### Google ADK: Event Iterator Pattern

```
User Request
    │
    ▼
┌─────────────────────────────┐
│ Runner.Run()                │
│ returns: iter.Seq2[Event]   │
└────────────┬────────────────┘
             │
             ▼
     ┌───────────────┐
     │ for each      │
     │ event, err    │
     │ := range ...  │
     └───────┬───────┘
             │
    ┌────────┴──────────┐
    │                   │
    ▼                   ▼
Event 1:            Event 2:
model.start         tool_call
    │                   │
    ▼                   ▼
Event 3:            Event 4:
tool_result         model.final
    │                   │
    └───────┬───────────┘
            │
            ▼
       Complete
       (all events yielded)
```

**Key Pattern**: Events yield naturally, iterator ends naturally

### adk-code (Current): Process Pattern

```
User Request
    │
    ▼
┌─────────────────────────────┐
│ Execute()                   │
│ returns: ExecutionResult    │
└────────────┬────────────────┘
             │
             ▼
    ┌────────────────┐
    │ Run to         │
    │ Completion     │
    └────────┬───────┘
             │
             ▼
    ┌────────────────────────┐
    │ Return Result:         │
    │  - Output string       │
    │  - Exit code           │
    │  - Duration            │
    └────────────────────────┘
```

**Problem**: Single output, no intermediate visibility

### adk-code (Required): Event Iterator Pattern

```
User Request
    │
    ▼
┌─────────────────────────────┐
│ Execute()                   │
│ returns: iter.Seq2[Event]   │
│ [REQUIRED CHANGE]           │
└────────────┬────────────────┘
             │
             ▼
     ┌───────────────┐
     │ for each      │
     │ event, err    │
     │ := range ...  │
     └───────┬───────┘
             │
    ┌────────┴──────────┐
    │                   │
    ▼                   ▼
Event 1:            Event 2:
agent.start         agent.running
    │                   │
    ▼                   ▼
Event 3:            Event 4:
agent.result        agent.complete
    │                   │
    └───────┬───────────┘
            │
            ▼
       Complete
       (all events yielded)
```

**Key Change**: Must match Google ADK's pattern

---

## Data Structure Comparison

### Google ADK: InvocationContext

```go
┌─────────────────────────────────────────┐
│     InvocationContext (interface)       │
├─────────────────────────────────────────┤
│                                         │
│  Core:                                  │
│  • Context() context.Context            │
│  • Agent() Agent                        │
│  • InvocationID() string                │
│                                         │
│  Session/History:                       │
│  • Session() session.Session            │
│  • OriginalInput() *genai.Content       │
│                                         │
│  State Management:                      │
│  • Memory() Memory                      │
│  • Artifacts() Artifacts                │
│  • User() User                          │
│                                         │
│  Navigation:                            │
│  • CallChain() []Agent                  │
│  • TransferAgent(Agent) error           │
│                                         │
│  Control:                               │
│  • EndInvocation(*genai.Content) error  │
│                                         │
└─────────────────────────────────────────┘
```

### adk-code (Current): ExecutionContext

```go
┌─────────────────────────────────────────┐
│     ExecutionContext (struct)           │
├─────────────────────────────────────────┤
│                                         │
│  Agent Spec:                            │
│  • Agent *Agent                         │
│  • Params map[string]interface{}        │
│                                         │
│  Execution Config:                      │
│  • Timeout time.Duration                │
│  • WorkDir string                       │
│  • Env map[string]string                │
│  • CaptureOutput bool                   │
│  • ReturnRawOutput bool                 │
│                                         │
│  Context:                               │
│  • Context context.Context              │
│                                         │
│  [MISSING: Session, Memory, Artifacts]  │
│                                         │
└─────────────────────────────────────────┘
```

### adk-code (Required): Expanded ExecutionContext

```go
┌─────────────────────────────────────────┐
│     ExecutionContext (expanded)         │
├─────────────────────────────────────────┤
│                                         │
│  Agent Spec:                            │
│  • Agent *Agent                         │
│  • Params map[string]interface{}        │
│                                         │
│  Execution Config:                      │
│  • Timeout time.Duration                │
│  • WorkDir string                       │
│  • Env map[string]string                │
│  • CaptureOutput bool                   │
│  • ReturnRawOutput bool                 │
│                                         │
│  Session/History: [NEW]                 │
│  • Session *session.Session             │
│  • InvocationID string                  │
│                                         │
│  State Management: [NEW]                │
│  • Memory Memory                        │
│  • Artifacts artifact.Service           │
│  • State session.State                  │
│                                         │
│  Context:                               │
│  • Context context.Context              │
│  • User string                          │
│  • FunctionCallID string                │
│  • EventActions *session.EventActions   │
│                                         │
└─────────────────────────────────────────┘
```

---

## Tool Pattern Comparison

### Google ADK: Tool Integration

```
┌────────────────────────────────────────────┐
│         Tool Interface                     │
├────────────────────────────────────────────┤
│ • Name() string                            │
│ • Description() string                     │
│ • IsLongRunning() bool                     │
└────────────────────────────────────────────┘
                    │
        ┌───────────┴────────────┐
        │                        │
        ▼                        ▼
┌──────────────────┐   ┌──────────────────┐
│ FunctionTool     │   │ AgentTool        │
│ (executable code)│   │ (agent as tool)  │
└──────────────────┘   └──────────────────┘
        │                        │
        └───────────┬────────────┘
                    │
                    ▼
            ┌──────────────┐
            │  Toolset     │
            │  Registry    │
            │  Management  │
            └──────────────┘
                    │
                    ▼
            ┌──────────────────┐
            │ Available to LLM │
            │ for invocation   │
            └──────────────────┘
```

### adk-code (Current): Incomplete

```
┌────────────────────────────────────────────┐
│         Tool Interface                     │
├────────────────────────────────────────────┤
│ ✅ Name() string                           │
│ ✅ Description() string                    │
│ ✅ IsLongRunning() bool                    │
└────────────────────────────────────────────┘
                    │
        ┌───────────┘
        │
        ▼
┌──────────────────────┐
│ FunctionTools        │
│ (file ops, search)   │
└──────────────────────┘
        │
        │  [MISSING: AgentTool]
        │  [INCOMPLETE: Registry]
        │
        ▼
    [Partial]
    (not all tools available)
```

### adk-code (Required): Complete

```
┌────────────────────────────────────────────┐
│         Tool Interface                     │
├────────────────────────────────────────────┤
│ ✅ Name() string                           │
│ ✅ Description() string                    │
│ ✅ IsLongRunning() bool                    │
└────────────────────────────────────────────┘
                    │
        ┌───────────┴────────────┐
        │                        │
        ▼                        ▼
┌──────────────────┐   ┌──────────────────┐
│ FunctionTools    │   │ AgentTools       │
│ ✅ Complete      │   │ [NEW]            │
└──────────────────┘   └──────────────────┘
        │                        │
        └───────────┬────────────┘
                    │
                    ▼
            ┌──────────────┐
            │ ✅ Registry  │
            │ [UPDATED]    │
            └──────────────┘
                    │
                    ▼
            ┌──────────────────┐
            │ Available to LLM │
            │ (agents + funcs) │
            └──────────────────┘
```

---

## Integration Points: Session Events

### Google ADK: Event Streaming

```
LLM Response
    │
    ▼
┌──────────────────────┐
│ Event Generated:     │
│ - Type: model.done   │
│ - Timestamp: now()   │
│ - Data: response     │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Session.AddEvent()   │
│ (event persisted)    │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Yield Event          │
│ (to iterator caller) │
└──────┬───────────────┘
       │
       ▼
    Complete
    (event visible in:
     - Session history
     - Iterator stream
     - Callbacks)
```

### adk-code (Current): No Event Streaming

```
Agent Execution
    │
    ▼
┌──────────────────────┐
│ Execute() runs       │
│ (no event tracking)  │
└──────┬───────────────┘
       │
       ▼
    [Blocking]
    (waiting for result)
       │
       ▼
┌──────────────────────┐
│ Return single result │
└──────┬───────────────┘
       │
       ▼
  Event was never
  created or persisted
  
  (not in session,
   not visible, not
   accessible to REPL)
```

### adk-code (Required): Event Streaming

```
Agent Execution Start
    │
    ▼
┌──────────────────────┐
│ Create Event:        │
│ - Type: agent.start  │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Session.AddEvent()   │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Yield Event          │
│ (to iterator)        │
└──────┬───────────────┘
       │
       ▼
Agent Execution
    │
    ▼
┌──────────────────────┐
│ Create Event:        │
│ - Type: agent.result │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Session.AddEvent()   │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Yield Event          │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│ Agent Complete       │
│ Yield final event    │
└──────┬───────────────┘
       │
       ▼
  Complete
  (all events in:
   - Session history
   - Iterator stream
   - Persisted)
```

---

## Timeline Comparison

### Google ADK: Delivery Timeline

```
2024
├── Q3: ADK-Go launch
├── Q4: Tool system
├── ...
└── Ongoing:
    ├── New models
    ├── More tools
    └── Enhancements

Status: ✅ Mature, production-ready
```

### adk-code: Delivery Timeline

```
Phase 0
├── 2024-10: Agent discovery
├── 2024-11: File parsing
└── Status: ✅ Complete

Phase 1
├── 2024-11: Configuration
├── 2024-12: Validation/Linting
└── Status: ✅ Complete

Phase 2 (IN PROGRESS)
├── 2025-01: Execution model
├── 2025-02: Tool integration
└── Status: 🔄 70% complete

Phase 3 (PLANNED)
├── 2025-03: Workflows
├── 2025-04: Metrics
└── Status: 📅 Not started
```

---

## Gap Closure Timeline

```
NOW (Nov 15)                PHASE 2 COMPLETE (Dec 15)
    │                              │
    │                              │
    ▼                              ▼
┌─────────────────────────────────────┐
│                                     │
│  Week 1: Core Refactoring           │
│  • ExecutionContext expansion       │
│  • Memory/Artifact interfaces       │
│  • Event-based Execute()            │
│  [Closes Gap #1]                    │
│                                     │
│  Week 2: Tool Integration           │
│  • Create agenttool package         │
│  • Agent tool registration          │
│  • CLI updates                      │
│  [Closes Gap #2, #3]                │
│                                     │
│  Week 3: Polish & Testing           │
│  • Comprehensive testing            │
│  • Documentation updates            │
│  • Performance verification         │
│  • Release prep                     │
│                                     │
│  Result: 100% alignment on          │
│  critical items, Phase 2 complete   │
│                                     │
└─────────────────────────────────────┘
```

---

## Risk Mitigation Roadmap

```
Risk Level
    │
    │
HIGH│   ┌─ Event Streaming
    │   │  Complexity
    │   │
MEDIUM│ ┌─ Session Integration
    │  │  ┌─ Performance
    │  │  │
LOW │  │  │  ┌─ Documentation
    │  │  │  │
    └──┴──┴──┴─────────── Time
    Week1   Week2   Week3

Mitigation:
• Extensive testing (all weeks)
• Reference Google ADK patterns
• Pair programming on complex parts
• Buffer time built in
```

---

## Alignment Scorecard

### Before Audit
```
Overall Alignment: ❓ UNKNOWN
  ├── Architecture: ❓
  ├── Patterns: ❓
  ├── APIs: ❓
  └── Integration: ❓
```

### After Audit + Fixes

```
Overall Alignment: ✅ FULL (target)
  ├── Architecture: ✅ Aligned
  │   └── Event-based execution
  ├── Patterns: ✅ Aligned
  │   └── Tool interface matching
  ├── APIs: ✅ Aligned
  │   └── ExecutionContext matches InvocationContext
  └── Integration: ✅ Aligned
      └── Session/memory/artifact systems
```

---

## Key Takeaway

```
┌──────────────────────────────────────────┐
│    Current Status                        │
├──────────────────────────────────────────┤
│ Alignment: 70%                           │
│ Gaps: 3 critical                         │
│ Effort: 3-4 weeks                        │
│ Risk: MODERATE (manageable)              │
├──────────────────────────────────────────┤
│    Post-Implementation (Target)          │
├──────────────────────────────────────────┤
│ Alignment: 100%                          │
│ Gaps: 0 critical                         │
│ Phase 2: ✅ COMPLETE                    │
│ Production Ready: ✅ YES                │
└──────────────────────────────────────────┘
```

---

**Document**: Visual Alignment Guide  
**Purpose**: Quick reference for architects and developers  
**Status**: Ready for distribution  
**Last Updated**: November 15, 2025
